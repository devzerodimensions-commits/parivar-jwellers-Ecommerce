{
  "name": "parivar-jewellers",
  "version": "1.0.0",
  "private": true,
  "description": "Parivar Jewellers — MERN jewellery store served as a single web service (backend serves the built frontend).",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix backend",
    "start": "node backend/server.js",
    "seed": "npm run seed --prefix backend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
